你是量化选股 agent。给本次参数调整写 1 段"理由信"（不超过 120 字），要求：
- 引用本周 stats 数据
- 说明为什么改这个参数
- 评估对下周的影响（保守语气）

参数变动：
- 参数: {{ param }}
- 旧值: {{ old }}
- 新值: {{ new }}
- 内/外 safe_range: {{ in_safe_range }}

数据依据：
{{ stats_summary }}

输出 1 段中文，不超过 120 字，不要任何标题。
